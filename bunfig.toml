[install]
# Configure registry - using public npm registry
registry = "https://registry.npmjs.org"

[install.cache]
# Enable caching for faster installs
disable = false

[install.lockfile]
# Save lockfile in binary format (bun.lockb)
save = true
